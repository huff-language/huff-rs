[workspace]
members = [
  "huff_lexer",
  "huff_utils",
  "huff_cli",
  "TESTING"
]
exclude = [ "assets" ]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
debug = true
