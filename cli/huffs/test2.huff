/* Interface */
#define function hello() payable returns ()

/* Storage Slots */
#define constant GREETER_LOCATION = FREE_STORAGE_POINTER()

#define macro CONSTRUCTOR() = takes(0) returns (0) {
    // Set msg.sender as the owner of the contract.
    OWNABLE_CONSTRUCTOR()
}

#define macro HELLO() = takes (0) returns (0) {
    [GREETER_LOCATION] sload        // [greeter]
    0x00 mstore                     // []
    0x20 0x00 return                // []
}